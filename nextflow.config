manifest {
    name = "Rob Syme test template"
}

params {
    inner = "my-example-inner-param"
    outer = "${params.inner}-outer"
}

process.container = 'quay.io/nextflow/bash'

profiles {
    test {
        params {
            inner = "test-inner-param"
            outer = "${params.inner}-testouter"
        }
    }
}